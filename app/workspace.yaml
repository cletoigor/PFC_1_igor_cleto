# workspace.yaml - Defines the code locations Dagster should load

load_from:
  - python_module:
      module_name: app.dagster.data_ingestion.assets
      working_directory: .
      # Optional: Specify the attribute name if it's not 'defs'
      # attribute: defs
      # Optional: Give the code location a specific name
      # location_name: tuya_pipeline_location
